apiVersion: platform.io/v1alpha1
kind: AWSBucket
metadata:
  name: my-first-cloud-bucket
  namespace: default
spec:
  parameters:
    bucketName: my-idp-test-bucket-998877 # Change this to something random!
    region: us-east-1
    isEncrypted: true
